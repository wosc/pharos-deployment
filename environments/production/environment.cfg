[environment]
connect_method = ssh
update_method = rsync
service_user = root

[hosts]
pharos.wosc.de =
    crontab, letsencrypt, mysql, nginx, phpbase, supervisor,
    basepackages, cronapt, nodejs,

    mailserver, mlmmj, roundcube, thyrida,
    pharos, calibreview, haemera, matomo, peerjs, radicale, shaarli, wallabag,

    prometheus, prom_alert, prom_node, grafana,
    prom_exim, prom_github, prom_mysql, prom_nginx, prom_syncthing,
    cgiserver, ddns, uptimerobot, webpasswd,

    woscde, dailystrips, controverse, roborally, seanopoly, tabu,
    grmusik, wordpress, wordpresscli,

    archivemail, fetchmail, rsspull, esniper, youtubedl,
    backup, sshauthorizedkeys, syncthing


[component:crontab]
mailto = wosc@wosc.de
