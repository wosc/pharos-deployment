[environment]
connect_method = ssh
update_method = rsync
service_user = root
platform = debian

[hosts]
pharos.wosc.de =
    crontab, mysql, nginx, phpbase, supervisor,
    basepackages, cronapt,

    mailserver, mlmmj, roundcube, thyrida,
    haemera, radicale,

    prometheus, prom_alert, prom_exim, prom_node,
    cgiserver, ddns, nginxdbauth, uptimerobot, webpasswd,

    tabu,

    archivemail, fetchmail, rsspull, esniper, twifeed, youtubedl,

    backup, unison


[component:crontab]
mailto = wosc@wosc.de
